```
from pathlib import Path
from bioinfokit import visuz,analys
import bioquest as bq
```

```
lfc=(0.585,0.585)
pv=(0.05,0.05)
df = bq.tl.deg_siglabel(deg_table,lfc_thr = lfc, padj = 'Pvalue', pv_thr = pv)
degs = bq.tl.deg_filter(df,top_n=10)
```

```
visuz.GeneExpression.volcano(
  df=deg_table.dropna()
  ,lfc='LFC'
  ,lfc_thr=lfc
  ,pv='Padj'
  ,pv_thr=pv
  ,sign_line=True
  ,geneid='Feature'
  ,genenames= tuple(degs.Feature.to_list() + ["ST6GAL2"])
  ,gstyle=2
  ,show=False
  ,plotlegend=True
  ,color=("red","grey","green")
  ,legendpos='upper right'
  ,legendanchor=(1.46,1)
  # ,figtype='pdf'
  # ,figname='output/11.火山图/NAFLD_volcano'
  )
export("火山图_deseq2_tumor_vs_normal")
```